# NewAPI 二次开发文档

本文档记录 RouterEasy 对 NewAPI 所做的最小化二次开发，便于后续同步上游版本时检查和保留必要改动。

## 2026-07-18：创建 Token 时返回 Token ID

### 修改文件

`controller/token.go`

### 修改内容

NewAPI 原有的 `POST /api/token/` 接口在创建 Token 成功后只返回成功状态：

```json
{
  "success": true,
  "message": ""
}
```

RouterEasy 增加新创建 Token 的数据库 ID：

```json
{
  "success": true,
  "message": "",
  "data": {
    "id": 123
  }
}
```

对应代码：

```go
"data": gin.H{"id": cleanToken.Id},
```

### 修改原因

SaaS 后端需要保存组织、创建成员与 NewAPI Token 的明确映射。Token ID 是稳定标识，不能通过 Token 名称推断创建结果，否则在名称重复或并发创建时可能关联错误。

### 兼容性

- 只增加 `data.id`，不修改已有响应字段。
- 不返回完整 API Key。
- NewAPI 原前端可以继续忽略新增字段。
- 属于向后兼容的最小修改。

### 验证

- `go test ./controller ./router` 通过。
- RouterEasy 已完成创建、列表、完整密钥读取和删除 Token 的本地联调。

### 上游同步注意事项

同步 NewAPI 上游版本后，检查 `controller.AddToken` 是否已经原生返回 Token ID。如果上游已经提供等价能力，应优先使用上游实现并删除本地重复补丁。

## 2026-07-18：自有使用日志支持 Token ID 过滤

### 修改文件

- `controller/log.go`
- `model/log.go`

### 修改内容

`GET /api/log/self` 新增可选查询参数 `token_ids`，使用英文逗号传入一个或多个 Token ID：

```text
/api/log/self?type=2&p=1&page_size=20&token_ids=2,5,7
```

接口仍先按当前登录的 NewAPI 用户过滤，再追加 `logs.token_id IN (...)` 条件。参数最多接受 100 个去重后的正整数 ID。

### 修改原因

RouterEasy 一个组织对应一个 NewAPI 用户，但组织内多个 SaaS 成员共享该用户。SaaS 普通成员只能查看自己创建的 API Key 产生的日志，需要在 NewAPI 分页和计数之前按 Token ID 过滤，避免 SaaS 事后过滤造成分页数量错误。

### 兼容性

- `token_ids` 不传时行为与原接口完全一致。
- 只修改 `/api/log/self`，不扩大管理员日志权限。
- 查询始终带当前 NewAPI `user_id` 条件，不能借此读取其他 NewAPI 用户的 Token 日志。

### 上游同步注意事项

同步上游后检查 `GetUserLogs` 的参数和查询实现。如果上游原生支持 Token ID 列表过滤，可改用上游参数并删除本地补丁。

## 2026-07-18：Claude 原生协议记录推理强度

### 修改文件

- `relay/claude_handler.go`
- `relay/claude_reasoning_test.go`

### 修改内容

Claude 原生 `/v1/messages` 请求完成模型后缀和 Thinking 适配后，将 `output_config.effort` 同步到 `RelayInfo.ReasoningEffort`。后续消费日志会通过现有逻辑写入 `other.reasoning_effort`。

### 修改原因

NewAPI 原有日志逻辑已经支持记录 `reasoning_effort`，但 Claude 原生协议只解析和转发了 `output_config.effort`，没有同步到日志上下文，导致 RouterEasy 使用记录中的推理强度为空。

### 兼容性

- 只记录请求实际携带或 NewAPI 模型后缀适配产生的 `effort`。
- 请求未指定推理强度时仍保持为空，不推断默认值。
- 只影响修改后的新日志，不修改历史日志。

### 上游同步注意事项

同步上游后检查 Claude 原生处理是否已经写入 `RelayInfo.ReasoningEffort`。如果上游提供等价实现，应删除本地重复补丁。

## 2026-07-18：自有使用日志汇总接口

### 修改文件

- `controller/log.go`
- `model/log.go`
- `router/api-router.go`

### 修改内容

新增 `GET /api/log/self/summary`，按当前登录的 NewAPI 用户汇总消费日志，支持 `token_ids`、`model_name`、`group`、`start_timestamp` 和 `end_timestamp` 筛选，返回请求数、输入 Token、输出 Token和实际扣除额度。

后续扩展该接口返回按模型分组的请求数、Token 和额度；同时新增 `GET /api/log/self/trend`，使用 PostgreSQL JSONB 聚合最近 7 天每天的输入、输出、缓存读取和缓存写入 Token。

### 修改原因

RouterEasy 总览需要按照 SaaS 组织成员创建的 Token ID 统计用量。原 `/api/log/self/stat` 不支持 Token ID 过滤，且其中 RPM/TPM 是最近一分钟指标，不能作为所选时间段的累计数据。

### 兼容性

- 新增独立接口，不修改现有日志接口行为。
- 强制使用当前登录的 NewAPI 用户 ID；传入 `token_ids` 时只统计指定 Token，自 2026-08-11 起省略该参数时统计当前用户的全部日志。
- 聚合在数据库完成，不向 SaaS 拉取明细日志。
- RouterEasy 部署固定使用 PostgreSQL，因此趋势接口直接使用 PostgreSQL JSONB 能力，不维护其他数据库实现。

### 上游同步注意事项

同步上游后检查是否已有支持 Token ID、模型、分组和时间范围的用户日志汇总接口。如有等价实现，应改用上游接口并删除本地补丁。

## 2026-07-19：管理员覆盖用户额度时同步 Redis 缓存

- `model/user.go` 新增 `SetUserQuota`，统一处理用户额度覆盖。
- PostgreSQL 更新成功后同步用户额度 Redis 缓存；缓存更新失败时使用户缓存失效，下一次读取回源数据库。
- `controller/user.go` 的 `add_quota / override` 改为调用该方法，避免数据库额度与运行时缓存短暂不一致。

## 2026-07-19：发布 VibeZoneX 自定义 GHCR 镜像

- 新增 `.github/workflows/routereasy-ghcr.yml`，仅允许手动触发。
- 镜像发布到 `ghcr.io/afei1029/new-api`，不使用上游 Docker Hub 仓库。
- 生产首期只构建 `linux/amd64`，同时生成版本标签和 Git 提交 SHA 标签。
- 生产 Compose 通过 OCI 摘要锁定镜像，避免同名标签变化导致不可预期升级。
- 工作流文件名和显示名仍含历史品牌 RouterEasy，待品牌文案统一调整时更名，不影响镜像内容。

## 2026-07-19：客户 API Key 鉴权返回稳定错误码

### 修改文件

- `middleware/auth.go`
- `middleware/auth_error_test.go`
- `types/error.go`

### 修改内容

Token 鉴权失败时根据密钥状态返回英文消息和稳定机器错误码：

```text
缺失或无效 -> invalid_api_key
过期         -> api_key_expired
禁用         -> api_key_disabled
额度耗尽     -> insufficient_token_quota
```

修改仅影响客户模型 API 的 Token 鉴权失败响应，不改变 NewAPI 管理后台的会话鉴权和内部国际化。

## 2026-07-27：网关错误响应统一使用英文

### 修改范围

- Relay 路由在进入网关处理链路时固定使用英文语言上下文。
- Token 鉴权、模型限流、额度校验、价格校验和异步任务等网关错误改为英文。
- 管理后台接口仍保留原有国际化行为，上游供应商返回的错误仍原样透传。

### 修改原因

模型 API 面向程序和国际用户。错误文案跟随浏览器或请求语言会造成同一错误返回不同语言，也可能让 SDK、日志告警和用户排障难以统一识别。

### 兼容性

- 不修改 HTTP 状态码和机器错误码。
- 不修改 NewAPI 管理后台页面语言。
- 不翻译或覆盖上游供应商的原始错误。

### 上游同步注意事项

同步上游后重点检查 Relay 路由的语言中间件、`middleware/auth.go`、`service/error.go`、`service/quota.go` 以及任务渠道错误。上游新增的网关错误也应保持英文。

## 2026-07-27：OpenAI Chat Completions / Responses 普通输入与缓存读取分开统计

### 修改文件

- `relay/channel/openai/relay_responses.go`
- `relay/channel/openai/relay_responses_compact.go`
- `relay/channel/openai/relay-openai.go`
- `relay/channel/openai/usage.go`
- `service/text_quota.go`
- `service/text_quota_test.go`
- `relay/channel/openai/relay_responses_usage_test.go`
- SaaS 后端 `internal/usage/service.go`

### 修改内容

OpenAI Chat Completions 和 Responses 返回的输入 Token 都包含缓存读取部分。结算仍以该原始总输入和缓存明细计算费用，但消费日志中的 `prompt_tokens` 改为：

```text
普通输入 = max(input_tokens - cached_tokens, 0)
```

同时在日志 `other` 中保留：

- `input_tokens_total`：上游原始总输入，用于审计。
- `cache_tokens`：缓存读取。
- `prompt_tokens_excludes_cache: true`：标记 `prompt_tokens` 已是普通输入。

SaaS 使用记录根据该标记避免重复扣减缓存；没有标记的历史日志继续使用原兼容逻辑。

### 计费安全

- `calculateTextQuotaSummary`、阶梯计费参数、额度结算和实际 `quota` 均继续使用原始总输入，没有改用展示字段。
- 只有 `RecordConsumeLog` 的 `PromptTokens` 使用普通输入。
- Chat Completions 和 Responses 使用相同的拆分口径；Claude 和 Gemini 统计口径不变。
- 缓存数异常大于总输入时，普通输入钳制为 `0`，不会产生负数。

### 上游同步注意事项

同步上游后检查 Chat Completions 和 Responses 用量是否已经区分普通输入和缓存读取。如果上游日志仍将缓存计入 `prompt_tokens`，必须保留本补丁，并确保结算链路仍使用原始用量快照。

## 2026-07-27：同步上游 v1.0.0-rc.22 后的 main

### 同步基线

- 本地共同基线：`a63364d156cf2a64f1c3d1ee4923d73d5f3222a1`
- 同步前本地主线：`e6c885fdb86ebc3cc243387d18a2243080623c16`
- 上游目标：`2cf3c8d71e92624c6e547c0be71250206ede0beb`
- 上游最新发布标签：`v1.0.0-rc.22`
- 同步范围：上游 `main` 新增 48 个提交。

### 上游主要变化

- 协议转换层抽离为可独立构建的 `relaykit` Go 模块。
- 默认前端从 `web/default` 迁移到 `web`，经典前端及旧日志清理入口被移除。
- 重构登录会话与认证管理，增加 New API、Sub2API 等渠道和独立工具计费。
- 增加每渠道 HTTP 传输控制、Responses Compact 扩展和多项网关修复。

### 二次开发保留与迁移

- 保留 SaaS 依赖的 `/api/log/self/summary` 和 `/api/log/self/trend` 接口。
- 保留网关错误统一英文的契约；上游新增的任务超时退款回归测试同步改为英文断言。
- 将 OpenAI Chat Completions / Responses 缓存用量规范化迁移到 `relaykit/dto` 类型，同时保留上游新增的实际工具调用和图像调用计费。
- 移除已无路由消费者、且依赖的模型函数已被上游删除的 `DeleteHistoryLogs`。
- 保留自有 GHCR 手动发布工作流 `.github/workflows/routereasy-ghcr.yml`。

### 验证

- `go test ./...` 通过。
- `cd relaykit && go test ./...` 通过。
- `cd web && bun run build:check` 通过类型检查和生产构建。
- `cd web && bun run lint` 仍有上游当前版本已存的全库 lint 错误；本次同步未扩大范围修改上游前端。

## 2026-08-01：同步上游 main 至 cfaba1dd

### 同步基线

- 同步前本地主线：`0a2916de96a79cb3b774a92571420b7e90a13efa`
- 上游起点：`2cf3c8d71e92624c6e547c0be71250206ede0beb`
- 上游目标：`cfaba1dd6754d4238e1360247c198a64a313e96c`
- 上游最新发布标签仍为：`v1.0.0-rc.22`
- 同步范围：上游 `main` 新增 16 个提交、修改 67 个文件。

### 上游主要变化

- 修复 `tiered_expr` 在自动分组重试时仍按初始分组倍率结算的问题，并覆盖免费组、付费组和钱包欠费语义。
- 增加 DeepSeek Responses API、Qwen `thinking_budget` 透传和 `zstd` 请求解压支持。
- 修复 New API 渠道的 multipart 图片编辑请求转换。
- 用户日志开放流式响应状态，增加 OIDC 自定义显示名称并修复 OAuth 回调窗口判断。
- 增加参数化慢 SQL 和数据库错误日志；`SQL_SLOW_THRESHOLD_MS` 为可选配置，不配置时默认 `200` 毫秒，配置为 `0` 可关闭慢查询日志。

### 二次开发保留情况

- 保留 SaaS 使用的日志筛选、汇总和趋势接口。
- 保留管理员覆盖用户额度时同步 Redis 缓存的修复。
- 保留稳定 API Key 错误码和网关错误统一英文的契约；本批上游新增网关路径未引入中文响应。
- 保留 OpenAI Chat Completions / Responses 普通输入与缓存读取拆分，同时继续使用原始总输入完成计费。
- 保留 RouterEasy GHCR 手动发布工作流。
- 上游自动分组重试计费修复与本地英文错误文案已合并，最终选中渠道后才刷新分组倍率，并在发送请求前补足预扣费。

### 部署影响

- 本次同步没有数据库迁移。
- 本次同步没有必填环境变量。
- `SQL_SLOW_THRESHOLD_MS` 为新增可选环境变量；线上可暂不配置并使用默认值。

### 验证

- `go test ./...` 通过。
- `cd relaykit && GOWORK=off go test ./...` 通过。
- `cd relaykit && GOWORK=off go build ./...` 通过。
- `cd web && bun run build:check` 通过类型检查和生产构建。
- 新增 OAuth 回调模式的 10 个前端测试全部通过。
- 本次新增或修改的前端文件定向 lint 未引入新错误；`public-header.tsx` 仍存在与本次两处字号调整无关的上游既有 lint 错误。
- 全库前端 lint 仍被上游既有错误阻断，本次同步未扩大范围修复原后台的历史 lint 债务。

## 2026-08-04：OpenAI 通用入口记录推理强度

### 修改文件

- `relay/compatible_handler.go`
- `relay/responses_handler.go`
- `relay/openai_reasoning.go`
- `relay/openai_reasoning_test.go`

### 修改内容

- Chat Completions 请求在进入渠道适配器前，从 `reasoning_effort` 捕获推理强度。
- Responses 请求在进入渠道适配器前，从 `reasoning.effort` 捕获推理强度。
- 捕获结果写入 `RelayInfo.ReasoningEffort`，沿用既有消费日志生成逻辑写入 `other.reasoning_effort`。
- 请求未提供有效推理强度时不写入，也不覆盖模型后缀或转换器已识别出的非空值。

### 修改原因

Sub2API 渠道适配器直接复用 New API 兼容适配器。该适配器会将 OpenAI Chat Completions 和 Responses 请求原样透传给上游，但原通用入口没有把请求中的推理强度同步到日志上下文。因此，上游供应商能够收到并展示 GPT/Codex 推理强度，NewAPI 本地消费日志却缺少 `reasoning_effort`；Claude 原生 `/v1/messages` 因已有独立捕获逻辑，不受该问题影响。

本次改为在协议通用入口统一捕获，不把修复限定在 Sub2API 适配器。这样所有透传型 OpenAI 兼容渠道都使用一致的日志行为，也避免为单个渠道复制记录逻辑。

### 兼容性

- 只补充消费日志元数据，不修改发往上游的请求体。
- 不修改模型映射、渠道调度、失败重试、Token 统计、额度预扣或最终计费。
- 不根据供应商响应或默认值猜测推理强度，只记录请求实际携带的非空值。
- Responses Compact 继续保持现有字段转发语义，不额外转发其未支持的 reasoning 字段。
- 只影响修复上线后产生的新日志，不回填历史日志。

### 验证

- `go test ./relay ./relay/channel/newapi ./relay/channel/sub2api` 通过。
- `git diff --check` 通过。
- 全仓测试中与 relay 相关的包通过；需要监听本机端口的既有 SMTP、Redis 和 HTTP 测试受本地沙箱 `bind` 权限限制，失败与本次修改无关。
- 生产发布版本：`v1.0.0-rc.22-custom.3`。
- 生产镜像 digest：`sha256:8cdcd4ca4c5a42feaae444d682b100e4da5d314a2dd6a52db2978f52a7ae200a`。
- 真实 Sub2API 类型 GPT/Codex Responses 请求验证通过，SaaS 最新使用记录已显示推理强度标签。

### 上游同步注意事项

同步上游后检查 Chat Completions 和 Responses 的通用处理入口是否已经将请求推理强度写入 `RelayInfo.ReasoningEffort`。如果上游提供等价实现，应删除本地 helper 和重复调用，但必须保留空值不覆盖已有非空推理强度的行为，并重新验证 Sub2API 或其他透传型兼容渠道。

## 2026-08-05：同步上游 v1.0.0-rc.23

### 同步基线

- 同步前上游基线：`cfaba1dd6754d4238e1360247c198a64a313e96c`。
- 上游目标：`0ab02020603d22e5613bc4cf46bfab06f8567769`。
- 上游最新发布标签：`v1.0.0-rc.23`。
- 同步范围：上游新增 2 个提交。

### 上游主要变化

- Token 使用 `auto` 分组时，可配置多个候选分组及其顺序；请求会按照 Token 保存的 Auto 分组顺序选择可用分组。
- AWS Bedrock 请求现在会随客户端连接断开而取消，减少客户端中止后仍占用上游连接和计算资源的情况。

### 二次开发保留与兼容性

- 保留 SaaS 使用的 Token 创建响应 `data.id`、日志筛选/汇总/趋势接口、稳定 API Key 错误码和网关错误统一英文的契约。
- 保留 OpenAI Chat Completions / Responses 的推理强度捕获，以及普通输入与缓存读取拆分和原始用量计费语义。
- 保留 RouterEasy GHCR 手动发布工作流和现有品牌定制。
- SaaS 当前创建的是明确的固定分组 Token，不使用 `auto` 分组，因此本次 Auto 分组顺序功能不会改变现有密钥的渠道选择和计费行为。
- 上游新增的三条 Auto 分组校验响应在简体中文和繁体中文后端语言包中统一改为英文，避免网关向用户返回中文错误；管理后台前端翻译继续保留本地化。

### 部署影响

- 本次同步没有数据库迁移。
- 本次同步没有新增必填环境变量。
- 建议生产镜像版本使用 `v1.0.0-rc.23-custom.1`。

### 验证

- `go test` 覆盖 `controller`、`middleware`、`model`、`service`、`setting`、`relay`、AWS、NewAPI/Sub2API 渠道和 `router`，全部通过。
- `cd relaykit && go test ./...` 通过。
- `cd web && bun run typecheck` 通过。
- `cd web && bun run build` 生产构建通过。
- Auto 分组顺序编辑器定向测试 7 项全部通过。
- 上游当前的 `api-key-group-cell.test.tsx` 仍期望渲染已在实现中注释掉的 Auto 徽标；该测试与实现的错位在 `upstream/main` 原样存在，不是本次合并或二次开发导致，未在本次发布中扩大修改范围。

## 2026-08-11：自有日志汇总与趋势支持查询全部历史

### 修改文件

`controller/log.go`

### 修改内容

`GET /api/log/self/summary` 和 `GET /api/log/self/trend` 的 `token_ids` 改为可选参数：

- 传入非空 `token_ids` 时，继续只汇总当前 NewAPI 用户下指定 Token 的日志。
- 省略 `token_ids` 时，不添加 Token ID 条件，汇总当前 NewAPI 用户的全部日志。
- 非法的 Token ID 格式仍然返回参数错误；趋势接口对时区参数的原有校验保持不变。

### 修改原因

SaaS 组织与 NewAPI 用户一一对应。API Key 在 SaaS 删除或成员被移除后，NewAPI 仍保留历史消费日志；如果汇总接口必须依赖当前有效的 Token ID 映射，组织总览会在删除 Key 后丢失历史请求量、Token 和费用统计。

SaaS 后端现在仅在“组织所有者查看整个组织，且未指定成员”时省略 `token_ids`；普通成员和指定成员筛选仍传入明确的 Token ID 列表。

### 权限与兼容性

- 两个接口仍强制使用当前认证的 NewAPI 用户 ID，省略 `token_ids` 不会扩大到其他 NewAPI 用户。
- 已有调用方继续传入 `token_ids` 时，请求和响应行为不变。
- 只放宽空筛选条件，不修改日志数据、聚合口径、额度结算或管理员权限。
- 本次没有 NewAPI 数据库迁移和环境变量变更。

### 发布与验证

- 提交：`c62ded8aaeac6dc75f2a0745c3be2709f34efb06`。
- 提交说明：`feat: support unfiltered self usage metrics`。
- 生产版本：`v1.0.0-rc.23-custom.2`。
- 生产镜像 digest：`sha256:629b5d0baf3a981424fc38d4981bac160f5fec9badb9c98e88af28c3f839a2a0`。
- `go test ./controller` 通过。
- 生产容器运行 revision 与提交一致，健康状态为 `healthy`，`/api/status` 返回新版本。
- 既有关键接口和全局 API 限流环境变量在容器重建后保持不变。

### 上游同步注意事项

同步上游后检查 `GetLogsSelfSummary` 和 `GetLogsSelfTrend` 的参数校验。如果上游仍要求 `token_ids` 非空，必须恢复本地可选语义，并验证省略该参数时 SQL 仍自动限定为当前认证用户。

## 2026-08-15：OpenAI 协议入站路径兼容省略 `/v1`

### 修改文件

- `main.go`
- `middleware/openai_path_compatibility.go`
- `middleware/openai_path_compatibility_test.go`

### 修改内容

在 HTTP Server 进入 Gin 路由匹配前增加 OpenAI 路径兼容层。客户端使用网关根地址作为 Base URL 时，NewAPI 会将明确支持的无版本 OpenAI 路径内部规范化为原有 `/v1/...` 路径：

```text
POST /chat/completions
-> POST /v1/chat/completions
```

已经携带 `/v1` 的请求保持原样，继续使用 NewAPI 原有路由、鉴权、模型分发、计费、日志和错误处理链路。

### 兼容范围

当前白名单覆盖 NewAPI 已实现的 OpenAI 协议接口：

- Chat Completions、Completions、Responses 和 Responses Compact。
- Embeddings、Moderations、Rerank 和 Alpha Search。
- 图片生成与编辑。
- 语音合成、转录与翻译。
- Models 列表与单模型查询。
- Realtime WebSocket。
- NewAPI 已有的 OpenAI 兼容视频创建、查询、内容获取和 remix 路由。

白名单同时校验 HTTP Method 和路径结构，不会把任意根路径泛化改写为 `/v1/*`。NewAPI 当前标记为 `RelayNotImplemented` 的 Files、Fine-tuning 和 Images Variations 等接口不纳入无 `/v1` 兼容，避免为不可用能力增加新入口。

### `/models` 路径冲突处理

NewAPI 控制台前端也使用根路径 `/models`，因此该路径按请求特征区分：

- 携带非空 `Authorization: Bearer <API Key>` 时，识别为 OpenAI API 请求并改写到 `/v1/models`。
- 未携带 Bearer 鉴权时，保留为控制台前端路由，继续返回页面入口。
- 兼容层只识别 Bearer 请求特征，API Key 是否真实、有效、启用以及额度是否充足，仍由原有 `TokenAuth` 统一校验。

### 边界与兼容性

- 路径规范化在 Gin 路由匹配前执行，避免复制原有 Relay 路由和中间件链。
- 同步改写 `URL.Path` 和 `RequestURI`，使后续路由识别、模型分发、上游转发及日志记录继续使用原有 `/v1` 语义。
- 支持白名单路径的 CORS `OPTIONS` 预检，预检的目标方法必须与已支持方法匹配。
- Claude `/v1/messages`、Gemini `/v1beta/*`、Kling、即梦、Midjourney、Suno 和管理接口 `/api/*` 不参与改写。
- 本次不修改渠道 Base URL 的上游路径拼接逻辑。渠道地址仍应按界面提示填写，不要为 OpenAI 兼容渠道重复添加 `/v1`。
- 本次无数据库迁移、无新增环境变量，也不改变任何计费和历史日志数据。

### 验证

- `go test ./...` 全量后端测试通过。
- 本地实际请求 `POST /chat/completions` 和 `POST /v1/chat/completions` 均进入同一 Relay 鉴权链，未携带有效 Key 时均返回 `401 invalid_api_key`。
- 无 `/v1` 的 CORS 预检返回 `204`。
- 未鉴权 `GET /models` 仍返回控制台 HTML，没有破坏前端路由。

### 上游同步注意事项

同步 NewAPI 上游版本后，检查 `main.go` 中 `http.Server.Handler` 是否仍通过 `middleware.OpenAIPathCompatibility` 包装 Gin Engine。如果上游新增或删除 OpenAI 协议路由，必须同步审核白名单和回归测试；不应改为对所有根路径统一添加 `/v1`。

## 2026-08-15：同步上游 v1.0.0-rc.24

### 同步基线

- 同步前上游基线：`0ab02020603d22e5613bc4cf46bfab06f8567769`（`v1.0.0-rc.23`）。
- 上游目标提交：`5c3abffe8572aa8a49f15c3916707d2019d66af4`。
- 上游发布标签：`v1.0.0-rc.24`。
- 本地合并提交：`726f34067962b627cc60599f401bb0b7ccccf63a`。
- 同步范围：上游新增 8 个提交；未追踪 `rc.24` 之后尚未发布的 `upstream/main`。

### 上游主要变化

- 为上游 HTTP 请求设置可重放的请求体，HTTP/2 连接在发送请求前遇到流重置时可由 Transport 透明重试。
- 请求体存储和各 Relay 入口改用统一可重放 Reader，并增加上游跳转、`GetBody` 与重试回归测试。
- 修复兑换码额度换算的精度损失。
- 为 Access Token 轮换和邀请额度转移增加用户级关键接口限流。
- 修正 Access Token 及用户属性并发更新时的旧快照覆盖风险。
- 改进渠道上游模型分类，渠道测试改用 Claude/Gemini 原生请求格式。

### 二次开发保留与兼容性

- 保留 SaaS 使用的 Token 创建响应 `data.id`、日志 Token ID 筛选/汇总/趋势以及空 `token_ids` 查询全部历史的契约。
- 保留稳定 API Key 错误码、网关错误统一英文和自定义 GHCR 发布工作流。
- 保留 OpenAI/Claude 推理强度记录、普通输入与缓存读取拆分，并继续使用原始总输入完成计费。
- 上游可重放请求体与上述日志和计费入口已自动合并，无文本冲突；请求语义仍由原有 Relay handler 处理。
- 本次 OpenAI 省略 `/v1` 兼容在 rc.24 合并后实施，上游未修改 `main.go`，因此 Server 入口包装无冲突。

### 验证

- `go test ./...` 根模块全量测试通过。
- `cd relaykit && GOWORK=off go test ./... && GOWORK=off go build ./...` 独立模块测试与构建通过。
- `cd web && bun run typecheck && bun run build` 类型检查与生产构建通过。
- rc.24 新增的兑换码编辑器定向测试 6 项全部通过。
- Web 全仓 `bun test` 仍有上次同步已记录的 Auto 徽标测试与当前实现不一致；另有部分既有 `node:test` 文件不支持被 Bun 聚合运行。这些问题与 rc.24 源码合并无关，本次未扩大修改范围。

### 部署影响

- 本次上游同步没有数据库迁移。
- 本次上游同步没有新增必填环境变量。
- 部署时只需构建新 NewAPI 镜像并按既有流程替换容器，不需要单独执行数据库或配置变更。

### 上游同步决策

`v1.0.0-rc.24` 之后的 `upstream/main` 又有 29 个未发布提交，涉及额度并发、充值结算、异步任务退款、Responses Compact 和推理强度等核心链路。本次不将这些未发布变更混入上线；待上游发布新正式标签后再单独评估、合并和回归。
