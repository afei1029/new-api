# NewAPI 二次开发文档

本文档记录 RouterEasy 对 NewAPI 所做的最小化二次开发，便于后续同步上游版本时检查和保留必要改动。

## 2026-07-18：创建 Token 时返回 Token ID

### 修改文件

`controller/token.go`

### 修改内容

NewAPI 原有的 `POST /api/token/` 接口在创建 Token 成功后只返回成功状态：

```json
{
  "success": true,
  "message": ""
}
```

RouterEasy 增加新创建 Token 的数据库 ID：

```json
{
  "success": true,
  "message": "",
  "data": {
    "id": 123
  }
}
```

对应代码：

```go
"data": gin.H{"id": cleanToken.Id},
```

### 修改原因

SaaS 后端需要保存组织、创建成员与 NewAPI Token 的明确映射。Token ID 是稳定标识，不能通过 Token 名称推断创建结果，否则在名称重复或并发创建时可能关联错误。

### 兼容性

- 只增加 `data.id`，不修改已有响应字段。
- 不返回完整 API Key。
- NewAPI 原前端可以继续忽略新增字段。
- 属于向后兼容的最小修改。

### 验证

- `go test ./controller ./router` 通过。
- RouterEasy 已完成创建、列表、完整密钥读取和删除 Token 的本地联调。

### 上游同步注意事项

同步 NewAPI 上游版本后，检查 `controller.AddToken` 是否已经原生返回 Token ID。如果上游已经提供等价能力，应优先使用上游实现并删除本地重复补丁。

## 2026-07-18：自有使用日志支持 Token ID 过滤

### 修改文件

- `controller/log.go`
- `model/log.go`

### 修改内容

`GET /api/log/self` 新增可选查询参数 `token_ids`，使用英文逗号传入一个或多个 Token ID：

```text
/api/log/self?type=2&p=1&page_size=20&token_ids=2,5,7
```

接口仍先按当前登录的 NewAPI 用户过滤，再追加 `logs.token_id IN (...)` 条件。参数最多接受 100 个去重后的正整数 ID。

### 修改原因

RouterEasy 一个组织对应一个 NewAPI 用户，但组织内多个 SaaS 成员共享该用户。SaaS 普通成员只能查看自己创建的 API Key 产生的日志，需要在 NewAPI 分页和计数之前按 Token ID 过滤，避免 SaaS 事后过滤造成分页数量错误。

### 兼容性

- `token_ids` 不传时行为与原接口完全一致。
- 只修改 `/api/log/self`，不扩大管理员日志权限。
- 查询始终带当前 NewAPI `user_id` 条件，不能借此读取其他 NewAPI 用户的 Token 日志。

### 上游同步注意事项

同步上游后检查 `GetUserLogs` 的参数和查询实现。如果上游原生支持 Token ID 列表过滤，可改用上游参数并删除本地补丁。

## 2026-07-18：Claude 原生协议记录推理强度

### 修改文件

- `relay/claude_handler.go`
- `relay/claude_reasoning_test.go`

### 修改内容

Claude 原生 `/v1/messages` 请求完成模型后缀和 Thinking 适配后，将 `output_config.effort` 同步到 `RelayInfo.ReasoningEffort`。后续消费日志会通过现有逻辑写入 `other.reasoning_effort`。

### 修改原因

NewAPI 原有日志逻辑已经支持记录 `reasoning_effort`，但 Claude 原生协议只解析和转发了 `output_config.effort`，没有同步到日志上下文，导致 RouterEasy 使用记录中的推理强度为空。

### 兼容性

- 只记录请求实际携带或 NewAPI 模型后缀适配产生的 `effort`。
- 请求未指定推理强度时仍保持为空，不推断默认值。
- 只影响修改后的新日志，不修改历史日志。

### 上游同步注意事项

同步上游后检查 Claude 原生处理是否已经写入 `RelayInfo.ReasoningEffort`。如果上游提供等价实现，应删除本地重复补丁。

## 2026-07-18：自有使用日志汇总接口

### 修改文件

- `controller/log.go`
- `model/log.go`
- `router/api-router.go`

### 修改内容

新增 `GET /api/log/self/summary`，按当前登录的 NewAPI 用户汇总消费日志，支持 `token_ids`、`model_name`、`group`、`start_timestamp` 和 `end_timestamp` 筛选，返回请求数、输入 Token、输出 Token和实际扣除额度。

后续扩展该接口返回按模型分组的请求数、Token 和额度；同时新增 `GET /api/log/self/trend`，使用 PostgreSQL JSONB 聚合最近 7 天每天的输入、输出、缓存读取和缓存写入 Token。

### 修改原因

RouterEasy 总览需要按照 SaaS 组织成员创建的 Token ID 统计用量。原 `/api/log/self/stat` 不支持 Token ID 过滤，且其中 RPM/TPM 是最近一分钟指标，不能作为所选时间段的累计数据。

### 兼容性

- 新增独立接口，不修改现有日志接口行为。
- 强制使用当前登录的 NewAPI 用户 ID，并要求至少传入一个 Token ID。
- 聚合在数据库完成，不向 SaaS 拉取明细日志。
- RouterEasy 部署固定使用 PostgreSQL，因此趋势接口直接使用 PostgreSQL JSONB 能力，不维护其他数据库实现。

### 上游同步注意事项

同步上游后检查是否已有支持 Token ID、模型、分组和时间范围的用户日志汇总接口。如有等价实现，应改用上游接口并删除本地补丁。

## 2026-07-19：管理员覆盖用户额度时同步 Redis 缓存

- `model/user.go` 新增 `SetUserQuota`，统一处理用户额度覆盖。
- PostgreSQL 更新成功后同步用户额度 Redis 缓存；缓存更新失败时使用户缓存失效，下一次读取回源数据库。
- `controller/user.go` 的 `add_quota / override` 改为调用该方法，避免数据库额度与运行时缓存短暂不一致。

## 2026-07-19：发布 VibeZoneX 自定义 GHCR 镜像

- 新增 `.github/workflows/routereasy-ghcr.yml`，仅允许手动触发。
- 镜像发布到 `ghcr.io/afei1029/new-api`，不使用上游 Docker Hub 仓库。
- 生产首期只构建 `linux/amd64`，同时生成版本标签和 Git 提交 SHA 标签。
- 生产 Compose 通过 OCI 摘要锁定镜像，避免同名标签变化导致不可预期升级。
- 工作流文件名和显示名仍含历史品牌 RouterEasy，待品牌文案统一调整时更名，不影响镜像内容。

## 2026-07-19：客户 API Key 鉴权返回稳定错误码

### 修改文件

- `middleware/auth.go`
- `middleware/auth_error_test.go`
- `types/error.go`

### 修改内容

Token 鉴权失败时根据密钥状态返回英文消息和稳定机器错误码：

```text
缺失或无效 -> invalid_api_key
过期         -> api_key_expired
禁用         -> api_key_disabled
额度耗尽     -> insufficient_token_quota
```

修改仅影响客户模型 API 的 Token 鉴权失败响应，不改变 NewAPI 管理后台的会话鉴权和内部国际化。

## 2026-07-27：网关错误响应统一使用英文

### 修改范围

- Relay 路由在进入网关处理链路时固定使用英文语言上下文。
- Token 鉴权、模型限流、额度校验、价格校验和异步任务等网关错误改为英文。
- 管理后台接口仍保留原有国际化行为，上游供应商返回的错误仍原样透传。

### 修改原因

模型 API 面向程序和国际用户。错误文案跟随浏览器或请求语言会造成同一错误返回不同语言，也可能让 SDK、日志告警和用户排障难以统一识别。

### 兼容性

- 不修改 HTTP 状态码和机器错误码。
- 不修改 NewAPI 管理后台页面语言。
- 不翻译或覆盖上游供应商的原始错误。

### 上游同步注意事项

同步上游后重点检查 Relay 路由的语言中间件、`middleware/auth.go`、`service/error.go`、`service/quota.go` 以及任务渠道错误。上游新增的网关错误也应保持英文。

## 2026-07-27：OpenAI Chat Completions / Responses 普通输入与缓存读取分开统计

### 修改文件

- `relay/channel/openai/relay_responses.go`
- `relay/channel/openai/relay_responses_compact.go`
- `relay/channel/openai/relay-openai.go`
- `relay/channel/openai/usage.go`
- `service/text_quota.go`
- `service/text_quota_test.go`
- `relay/channel/openai/relay_responses_usage_test.go`
- SaaS 后端 `internal/usage/service.go`

### 修改内容

OpenAI Chat Completions 和 Responses 返回的输入 Token 都包含缓存读取部分。结算仍以该原始总输入和缓存明细计算费用，但消费日志中的 `prompt_tokens` 改为：

```text
普通输入 = max(input_tokens - cached_tokens, 0)
```

同时在日志 `other` 中保留：

- `input_tokens_total`：上游原始总输入，用于审计。
- `cache_tokens`：缓存读取。
- `prompt_tokens_excludes_cache: true`：标记 `prompt_tokens` 已是普通输入。

SaaS 使用记录根据该标记避免重复扣减缓存；没有标记的历史日志继续使用原兼容逻辑。

### 计费安全

- `calculateTextQuotaSummary`、阶梯计费参数、额度结算和实际 `quota` 均继续使用原始总输入，没有改用展示字段。
- 只有 `RecordConsumeLog` 的 `PromptTokens` 使用普通输入。
- Chat Completions 和 Responses 使用相同的拆分口径；Claude 和 Gemini 统计口径不变。
- 缓存数异常大于总输入时，普通输入钳制为 `0`，不会产生负数。

### 上游同步注意事项

同步上游后检查 Chat Completions 和 Responses 用量是否已经区分普通输入和缓存读取。如果上游日志仍将缓存计入 `prompt_tokens`，必须保留本补丁，并确保结算链路仍使用原始用量快照。
